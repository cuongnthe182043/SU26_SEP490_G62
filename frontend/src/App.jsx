import React from "react";

export default function App() {
  return (
    <div style={{ padding: 20 }}>
      <h1>Frontend React App</h1>
      <p>
        This is a minimal React skeleton. Run `npm install` then `npm start`.
      </p>
    </div>
  );
}
